# 🚀 Быстрый старт TaskMaster

## За 5 минут до запуска!

### 1️⃣ Установка (автоматически)

```bash
# Запустите установочный скрипт
./setup.sh
```

Или вручную:
```bash
npm run install-all
```

### 2️⃣ Настройка MongoDB

**Вариант A: MongoDB Atlas (рекомендуется для начала)**

1. Перейдите на [MongoDB Atlas](https://cloud.mongodb.com)
2. Создайте бесплатный кластер (нажмите "Build a Database" → "Free")
3. Создайте пользователя (Database Access → Add New Database User)
4. Добавьте сеть (Network Access → Add IP Address → "Allow Access from Anywhere")
5. Скопируйте connection string (Connect → Connect your application)

**Вариант B: Локальный MongoDB**

Если у вас установлен MongoDB локально:
```
MONGODB_URI=mongodb://localhost:27017/taskmaster
```

### 3️⃣ Обновите .env файл

Откройте `.env` в корне проекта и вставьте ваш MongoDB URI:

```env
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster.mongodb.net/taskmaster

JWT_SECRET=mySecretKey123456789  # Можете оставить как есть для разработки

PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000
```

> **Важно**: Замените `YOUR_USERNAME` и `YOUR_PASSWORD` на реальные значения!

### 4️⃣ Запуск

```bash
# Запустите backend + frontend одновременно
npm run dev-all
```

Готово! 🎉

- Frontend: http://localhost:3000
- Backend API: http://localhost:5000/api

### 5️⃣ Первое использование

1. Откройте http://localhost:3000
2. Нажмите **"Sign up"**
3. Зарегистрируйтесь (username, email, password)
4. Готово! Вы автоматически войдёте в систему

### 🎯 Что дальше?

- Создайте свою первую задачу
- Изучите Dashboard со статистикой
- Попробуйте фильтры и поиск
- Добавьте комментарии к задачам

### 🐛 Проблемы?

**Backend не запускается:**
```bash
# Проверьте, что MongoDB URI корректный
# Убедитесь, что порт 5000 свободен
```

**Frontend не видит Backend:**
```bash
# Убедитесь, что backend запущен
# Проверьте frontend/.env содержит:
# VITE_API_URL=http://localhost:5000/api
```

**Ошибка подключения к MongoDB:**
- Проверьте username и password
- Убедитесь, что IP адрес в whitelist
- Попробуйте добавить `&authSource=admin` в конец URI

### 📚 Полная документация

Смотрите [FRONTEND_README.md](./FRONTEND_README.md) для детальной информации.

---

**Успехов! 🚀**
