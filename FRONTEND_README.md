# TaskMaster - Full Stack Task Management System

Современная система управления задачами с красивым React frontend и мощным Node.js/Express backend.

## 🚀 Технологический стек

### Backend
- **Node.js** & **Express.js** - REST API сервер
- **MongoDB** - База данных
- **JWT** - Аутентификация
- **Bcrypt** - Хеширование паролей
- **Nodemailer** - Email уведомления
- **Joi** - Валидация данных

### Frontend
- **React 18** - UI библиотека
- **Vite** - Быстрая сборка
- **React Router** - Навигация
- **Axios** - HTTP клиент
- **Recharts** - Графики и визуализация
- **Lucide React** - Иконки

## ✨ Основные возможности

### Для пользователей
- 🔐 Регистрация и авторизация с JWT
- 📊 Интерактивный dashboard со статистикой
- ✅ Полное управление задачами (CRUD)
- 🎯 Фильтрация по статусу, приоритету и категории
- 🔍 Поиск задач
- 💬 Система комментариев
- ⚙️ Настройки профиля и предпочтений
- 📧 Email уведомления

### Для администраторов
- 👥 Управление пользователями
- 🛡️ Изменение ролей (User, Premium, Admin)
- 📈 Статистика пользователей

### RBAC (Role-Based Access Control)
- **User** - до 20 задач, базовые функции
- **Premium** - безлимитные задачи, все функции
- **Admin** - полный доступ + управление пользователями

## 📦 Установка

### Требования
- Node.js >= 18.0.0
- MongoDB Atlas аккаунт (бесплатный)
- Brevo (SendinBlue) аккаунт для email (опционально)

### Шаг 1: Клонирование и установка зависимостей

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd Task_Master_Final-main

# Установите все зависимости (backend + frontend)
npm run install-all
```

### Шаг 2: Настройка MongoDB

1. Перейдите на [MongoDB Atlas](https://cloud.mongodb.com)
2. Создайте бесплатный кластер (M0 Sandbox)
3. Создайте пользователя базы данных
4. Добавьте IP адрес в whitelist (или 0.0.0.0/0 для разработки)
5. Получите connection string

### Шаг 3: Настройка переменных окружения

Отредактируйте `.env` файл в корне проекта:

```env
# MongoDB
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskmaster

# JWT
JWT_SECRET=your_very_secure_secret_key_here

# Server
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# Email (опционально - для уведомлений)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_brevo_smtp_key
FROM_EMAIL=noreply@taskmaster.com
FROM_NAME=TaskMaster
```

### Шаг 4: Настройка Email (Опционально)

Для email уведомлений:

1. Зарегистрируйтесь на [Brevo](https://app.brevo.com)
2. Перейдите в Settings → SMTP & API
3. Создайте SMTP ключ
4. Обновите SMTP настройки в `.env`

Бесплатный лимит: 300 email/день

## 🎯 Запуск приложения

### Вариант 1: Запуск всего приложения (рекомендуется)

```bash
# Запустить backend + frontend одновременно
npm run dev-all
```

Backend будет доступен на `http://localhost:5000`  
Frontend будет доступен на `http://localhost:3000`

### Вариант 2: Запуск по отдельности

**Backend:**
```bash
npm run dev
```

**Frontend (в другом терминале):**
```bash
npm run client
```

### Вариант 3: Production режим

```bash
# Backend
npm start

# Frontend (собрать для production)
cd frontend
npm run build
```

## 📱 Использование приложения

### Первый запуск

1. Откройте `http://localhost:3000`
2. Нажмите "Sign up" для регистрации
3. Создайте аккаунт (username, email, password)
4. Автоматический вход после регистрации

### Основные функции

#### Dashboard
- Просмотр статистики задач
- Графики распределения по статусам и приоритетам
- Процент выполнения
- Список задач с высоким приоритетом

#### Tasks (Задачи)
- **Создание**: Кнопка "New Task"
- **Редактирование**: Иконка карандаша на карточке
- **Удаление**: Иконка корзины
- **Комментарии**: Иконка сообщения
- **Фильтрация**: По статусу, приоритету, категории
- **Поиск**: Строка поиска вверху страницы

#### Profile (Профиль)
- Обновление username и email
- Настройки уведомлений
- Выбор темы
- Выбор языка

#### Admin (только для администраторов)
- Просмотр всех пользователей
- Изменение ролей
- Удаление пользователей
- Статистика по ролям

## 🎨 Особенности дизайна

- 🎯 Современный и чистый UI
- 📱 Полностью адаптивный дизайн (mobile-first)
- 🎨 Градиентные акценты
- ⚡ Плавные анимации и переходы
- 🌈 Цветовая схема на основе статусов
- 📊 Интерактивные графики с Recharts

## 🔒 Безопасность

- Хеширование паролей с bcrypt (salt rounds: 10)
- JWT токены для аутентификации
- Rate limiting для API endpoints
- CORS защита
- Валидация данных с Joi
- XSS защита
- Безопасное хранение паролей (never selected by default)

## 📝 API Endpoints

### Authentication
```
POST /api/auth/register - Регистрация
POST /api/auth/login    - Вход
```

### Tasks
```
GET    /api/tasks           - Получить все задачи
GET    /api/tasks/:id       - Получить задачу
POST   /api/tasks           - Создать задачу
PUT    /api/tasks/:id       - Обновить задачу
DELETE /api/tasks/:id       - Удалить задачу
GET    /api/tasks/search    - Поиск задач
```

### Dashboard
```
GET /api/dashboard - Статистика dashboard
```

### Comments
```
GET    /api/comments/:taskId  - Комментарии задачи
POST   /api/comments/:taskId  - Добавить комментарий
```

### User
```
GET /api/users/profile    - Получить профиль
PUT /api/users/profile    - Обновить профиль
```

### Preferences
```
GET /api/preferences - Получить настройки
PUT /api/preferences - Обновить настройки
```

### Admin
```
GET    /api/admin/users        - Все пользователи
PUT    /api/admin/users/:id/role - Изменить роль
DELETE /api/admin/users/:id     - Удалить пользователя
```

## 🐛 Устранение неполадок

### Frontend не подключается к Backend

Проверьте:
1. Backend запущен на порту 5000
2. Frontend `.env` содержит `VITE_API_URL=http://localhost:5000/api`
3. CORS настроен правильно в `server.js`

### MongoDB connection error

1. Проверьте `MONGODB_URI` в `.env`
2. Убедитесь, что IP адрес в whitelist
3. Проверьте правильность username/password

### Email не отправляются

1. Email функционал опционален - приложение работает без него
2. Проверьте SMTP настройки в `.env`
3. Убедитесь в корректности Brevo API ключа

## 🚀 Деплой

### Backend (Render.com)

1. Создайте Web Service на Render
2. Подключите GitHub репозиторий
3. Настройки:
   - Build Command: `npm install`
   - Start Command: `npm start`
4. Добавьте environment variables из `.env`

### Frontend (Vercel/Netlify)

1. Соберите frontend: `cd frontend && npm run build`
2. Деплой папки `frontend/dist`
3. Обновите `VITE_API_URL` на production URL

## 👥 Команда разработки

- **Chingiz** - Authentication & User Management
- **Kaysar** - Task Management & Validation  
- **Sultan** - RBAC, Email Service & Deployment

## 📄 Лицензия

MIT

## 🤝 Поддержка

Для вопросов и предложений создайте issue в репозитории.

---

**Приятной работы с TaskMaster! 🚀**
